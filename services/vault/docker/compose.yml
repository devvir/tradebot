# Vault Service — the writer instance. Owns raw dump file storage.
# Read-only replicas extend base.yml directly (see the farm module's harvester).

services:
  vault:
    extends:
      file: base.yml
      service: vault

    container_name: vault

    ports:
      - ${VAULT_PORT:-}:8000
      - :3000
