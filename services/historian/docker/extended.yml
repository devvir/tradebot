# Historian Extended Service
# Includes: mongodb (database), historian (BitMEX REST data fetcher)

include:
  - path: ../../mongodb/docker/compose.yml

services:
  historian:
    extends:
      file: ./compose.yml
      service: historian

    depends_on:
      mongodb:
        condition: service_healthy
