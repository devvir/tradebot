{
  "name": "@tradebot/executor",
  "version": "1.0.0",
  "description": "Order executor — receives desired state and reconciles live orders to match",
  "main": "dist/src/index.js",
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/src/index.js",
    "dev": "ts-node src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@devvir/bitmex-database": "^1.0.0",
    "@devvir/service-kit": "workspace:*",
    "@tradebot/utils": "workspace:*",
    "express": "^5.2.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0"
  }
}
