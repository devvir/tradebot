{
  "name": "@tradebot/codec",
  "version": "1.0.0",
  "description": "Codec service - consumes BitMEX messages, transforms them, and publishes them to another exchange",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/src/index.js",
    "dev": "ts-node src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@devvir/rabbitmq": "workspace:*",
    "@devvir/service": "workspace:*",
    "@tradebot/utils": "workspace:*",
    "bson": "^7.2.0"
  }
}
