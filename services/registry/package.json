{
  "name": "@tradebot/registry",
  "version": "1.0.0",
  "description": "Registry service — persistent ordered mappings for symbols and currencies",
  "main": "dist/src/index.js",
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/src/index.js",
    "dev": "ts-node src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@devvir/service-kit": "workspace:*",
    "@tradebot/utils": "workspace:*",
    "express": "^5.2.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "mongodb":          "^7.1.1",
    "@types/express":   "^5.0.0",
    "@types/supertest": "^6.0.3",
    "supertest":        "^7.1.0"
  }
}
