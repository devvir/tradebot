{
  "name": "@tradebot/farmer",
  "version": "1.0.0",
  "description": "Reads closed vault files, reconstructs WS envelopes, and inserts the data into MongoDB",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/src/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@devvir/service-kit": "workspace:*",
    "@tradebot/types": "workspace:*",
    "@tradebot/utils": "workspace:*",
    "mongodb": "^7.1.0",
    "undici": "^8.2.0"
  }
}
