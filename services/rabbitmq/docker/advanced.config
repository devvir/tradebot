[
  {rabbit, [
    %% How many messages a connection process can send to a queue process before
    %% waiting for a credit grant. Default {400, 200} is conservative.
    %% Higher values reduce round-trip overhead for high-throughput forwarding.
    {credit_flow_default_credit, {4000, 2000}}
  ]}
].
