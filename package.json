{
  "name": "tradebot-monorepo",
  "version": "0.1.0",
  "description": "BitMEX Trading Bot",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^22.22.0"
  },
  "scripts": {
    "build": "pnpm --color -r build",
    "lint": "pnpm --color -r lint",
    "test": "pnpm --color -r test",
    "dev": "pnpm --color -r dev",
    "format": "pnpm --color -r format",
    "check": "pnpm --color -r check"
  },
  "devDependencies": {
    "@nx/vitest": "22.5.4",
    "@tradebot/types": "workspace:*",
    "@types/amqplib": "^0.10.8",
    "@types/node": "^25.5.0",
    "@types/ws": "^8.18.1",
    "@vitest/coverage-v8": "^4.1.0",
    "knip": "^5.88.1",
    "nx": "22.5.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vitest": "^4.1.0"
  }
}
