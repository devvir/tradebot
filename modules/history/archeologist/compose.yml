# Archeaologist Module
# Includes: historian service (fetcher)
#
# Infra: db-store, bouncer
#
# Fetches historical paginated data from BitMEX REST API and persists it.

# Docker Compose Project (for all history app modules)
name: history

include:
  - path: ../../../services/historian/docker/compose.yml

networks:
  default:
    name: ${PROJECT_NAME:-tradebot}-network
    external: true
